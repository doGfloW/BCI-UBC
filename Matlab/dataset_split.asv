% splits dataset into training and testing
clear all
close all
[file,path] = uigetfile('*.xlsx','*.xlx');
read_data=table2array(readtable(file));
[m,n] = size(data) ;
P = 0.80 ;
idx = randperm(m)  ;
Training = data(idx(1:round(P*m)),:) ; 
Testing = data(idx(round(P*m)+1:end),:) ;